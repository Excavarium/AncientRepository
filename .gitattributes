# Game dump: store files exactly as processed (no CRLF/LF rewriting).
# That avoids noisy "CRLF will be replaced by LF" warnings and keeps bytes stable.
* -text

# Repo docs only — normalize for readability
*.md text eol=lf
.gitignore text eol=lf
.gitattributes text eol=lf
